# scripts/ — developer tooling

| Script | Purpose |
|--------|---------|
| `generate_icons.py` | Pillow: builds PNG/SVG icons into `../icons/` |

Run from repo root: `python scripts/generate_icons.py`.

Keep scripts optional for end users (app itself needs no build). Document new tools here when added.
